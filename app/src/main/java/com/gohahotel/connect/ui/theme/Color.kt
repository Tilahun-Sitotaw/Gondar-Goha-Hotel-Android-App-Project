package com.gohahotel.connect.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Goha Hotel Brand Palette ─────────────────────────────────────────────────
// Primary: Warm Gold (luxury, Ethiopian heritage)
val GoldPrimary       = Color(0xFFD4A843)
val GoldLight         = Color(0xFFEDC96A)
val GoldDark          = Color(0xFFA07820)

// Secondary: Deep Teal (sophistication, African sky)
val TealPrimary       = Color(0xFF1A5C6B)
val TealLight         = Color(0xFF2A7A8C)
val TealDark          = Color(0xFF0D3A45)

// Tertiary: Terracotta (Ethiopian earth tones)
val TerracottaPrimary = Color(0xFFB85C38)
val TerracottaLight   = Color(0xFFD47A55)
val TerracottaDark    = Color(0xFF8A3A1F)

// Neutrals – Dark Mode (Glassmorphism Support)
val SurfaceDark       = Color(0xFF0A1114) // Deeper rich black-teal
val SurfaceVariantDark= Color(0x661A2D33) // 40% opacity for glass effect
val CardDark          = Color(0x801E3540) // 50% opacity for glass cards
val OnSurfaceDark     = Color(0xFFFDFBF7)
val OutlineDark       = Color(0x4D3A5560) // 30% opacity

// Neutrals – Light Mode (Premium Clean)
val SurfaceLight      = Color(0xFFFDFCF8)
val SurfaceVariantLight = Color(0x80EFE8D0) // 50% opacity
val CardLight         = Color(0xCCFFFFFF) // 80% opacity white
val OnSurfaceLight    = Color(0xFF141C1F)
val OutlineLight      = Color(0x4DB0A080)

// Semantic
val SuccessGreen      = Color(0xFF2E7D32)
val WarningAmber      = Color(0xFFFF8F00)
val ErrorRed          = Color(0xFFD32F2F)
val InfoBlue          = Color(0xFF1565C0)

// Status colours for order tracking
val StatusReceived    = Color(0xFF607D8B)
val StatusPreparing   = Color(0xFFFF8F00)
val StatusOnWay       = Color(0xFF1565C0)
val StatusDelivered   = Color(0xFF2E7D32)
